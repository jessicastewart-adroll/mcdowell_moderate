def get_max(a, b):
  # assumes positive integers
  counts = [[] for i in range(abs(a-b))]
  pass
  
def get_max_neg(a, b):
  # assumes positive or negative integers
  pass

def get_max_float(a, b):
  # assumes positive or negative integers or floats
  pass
